const initialState = {
    blogs:[]
  }
  
  function blogReducer(state = initialState, action) {

    switch (action.type) {
      
      default:
        return state;
    }
  }
  
  export default blogReducer